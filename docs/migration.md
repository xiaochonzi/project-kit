# 迁移说明

本仓库的架构演进:

1. **单一 skill 路由**(初始):一个 SKILL.md 内部路由 10 个模式
2. **薄壳 + 共享**(第一轮重构):10 个薄壳技能引用中央 `shared/`,已被废弃
3. **自包含技能**(当前):每个 SKILL.md 内嵌完整流程,删除 `shared/`、`workflows/`、`references/`,模板归 `assets/templates/` 由脚本使用

废弃原因:薄壳 + 相对路径引用在 Claude Code 安装后无法从用户项目目录解析,技能读不到共享文档;自包含保证技能在任何 cwd 可用。
