---
description: 独立验收已完成的 Full Plan，并用新鲜证据收口状态
argument-hint: "<change-id>"
---

读取并严格执行 `skills/verify-plan/SKILL.md` 对应的 `verify-plan` 技能。`$ARGUMENTS` 必须提供 Change ID（如 `CR-001`）。

只负责路由；Spec、Plan、条件技术设计、代码规范和状态收口全部以该技能为准。
