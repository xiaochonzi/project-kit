---
description: 执行已批准的 Full Plan，逐任务实施并记录验证证据
argument-hint: "<change-id>"
---

读取并严格执行 `skills/execute-plan/SKILL.md` 对应的 `execute-plan` 技能。`$ARGUMENTS` 必须提供 Change ID（如 `CR-001`）。

只负责路由；Plan 前置核对、TDD、范围限制、任务勾选、验证记录和状态迁移全部以该技能为准。
