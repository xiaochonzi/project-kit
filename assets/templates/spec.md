---
change: {{CHANGE}}
title: {{TITLE}}
status: draft
created_at: {{DATE}}
---

# {{TITLE}}

## 术语与业务对象

<!-- 由 spec 技能填写：定义可能影响理解的术语和对象；没有新增含义时说明沿用哪些现有定义。
     细节度：每个 ID/概念定义到唯一无歧义，例如“`<对外 ID>` = `<具体数据表>.<字段>`”，不允许“`<某个 ID>` 到底指哪个 ID”的歧义。
     数据权威表：用一个表格列出每个业务事实的唯一权威（如“当前用户/租户是谁 → 登录上下文”“内容是否属于任务 → 对应数据表”）。 -->

## 问题与依据

<!-- 由 spec 技能填写：当前状态、问题证据和触发原因。 -->

## 目标

<!-- 由 spec 技能填写：一个具体、可观察的目标结果。 -->

## 用户流程

<!-- 由 spec 技能填写：角色、触发条件、前置条件、主流程和结果。
     细节度：流程必须覆盖每个分支（正常 + 异常），不只写 happy path。
     红线：参与者只能是业务角色（用户/前端/后端/领域系统），禁止出现类名、包名、技术调用链。 -->

## 范围

### 包含

<!-- 由 spec 技能填写。 -->

### 不包含

<!-- 由 spec 技能填写：列出相邻但明确不做的行为及原因。 -->

## 输入与输出

<!-- 由 spec 技能填写：业务输入、输出和对外契约；不适用时写明原因。
     细节度：写到对外 API 的字段名、类型、语义、错误码、幂等键，以及成功/失败/边界三种响应变体，不只写 happy path。
     红线：只写对外契约（wire format），不写内部 DTO/VO 类名、方法签名、文件路径。 -->

## 业务规则

<!-- 由 spec 技能填写。每项需求使用 REQ-01，每条规则使用 BR-01。BR 用决策表表达，不用散文。
     规范关键词：全文使用 必须 / 不得 / 应该 / 服务端权威（RFC 2119 风格），禁止“适当处理”“体验良好”“正常工作”。 -->

- BR-01：<业务规则>

### REQ-01：<需求名称>

- Current：<当前行为>
- Trigger：<触发条件>
- Target：<目标行为>
- Business Rules：BR-01
- Acceptance：AC-01

## 失败与边界情况

<!-- 由 spec 技能填写：无效、空值、边界值、重复、越权、部分失败等适用情况。
     细节度：逐条写死，每条给“必须行为”（情况 → 必须行为 的决策表）。 -->

## 禁止事项

<!-- 由 spec 技能填写：明确系统不得发生的行为；不得使用需要原始对话解释的模糊引用。 -->

## 验收标准

<!-- 由 spec 技能填写：每条必须可得到 PASS/FAIL，含具体输入、动作和可观察结果。
     细节度：覆盖三种结果（未完成/有下一项/全部完成），不只写成功样例。
     红线：编号不得有字母后缀变体（如 AC 后接字母的编号，应重编号为下一个整数）。 -->

- [ ] AC-01：<可得到 PASS/FAIL 的验收条件>

## 未决问题

<!-- 由 spec 技能填写；批准前必须精确写为“无”。
     交付前自检（不留正文）：
     - 一个不参与原始讨论的人，能否仅凭本文写出测试，且无第二种合理解释？
     - 全文是否出现类名/包名/文件路径（属 plan）？出现即越界，删除。 -->
